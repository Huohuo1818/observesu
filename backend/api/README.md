# API entry

This directory is reserved for future public and internal API routes.

Planned initial routes:

- `GET /health`
- `GET /feishu/setup`
- `POST /webhooks/feishu`
- `POST /tasks/site-update`
- `GET /tasks/:id`
