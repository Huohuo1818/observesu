# OpenAI integration entry

This directory is reserved for structured OpenAI calls used by the Feishu bot and future backend APIs.

Planned responsibilities:

- Convert site-edit requests into structured prompts
- Generate safe content/code update payloads
- Return machine-readable output for GitHub PR creation
- Keep deployment-triggering actions behind reviewable PRs
