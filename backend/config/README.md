# Config entry

This directory is reserved for runtime configuration, environment parsing, and integration-specific settings.

Keep secrets in environment variables and never commit real credentials.
